<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Backup and device-to-device transfer are disabled on purpose. The app stores a long-lived
         Supabase refresh token (biometric_refresh_token, see DataStoreGoTrueSessionManager) that
         survives restarts so fingerprint login can work, and BiometricPrompt runs without a
         CryptoObject — so a data dir restored onto another phone lets THAT phone's fingerprint
         unlock the previous user's account. Everything else here is a cache of Supabase and
         re-syncs after login, so a backup preserves nothing worth the risk. Do not re-enable.
         The two rules files below must stay in sync with this flag. -->
    <application
        android:name=".PedidosApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcomerceCarlosV">

        <!-- Disable only the WorkManager auto-init so Hilt's HiltWorkerFactory is used instead.
             Use tools:node="remove" on the meta-data entry only — removing the whole
             InitializationProvider would kill other androidx.startup initializers (e.g.
             multiplatform-settings SettingsInitializer). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.EcomerceCarlosV.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
